type User {
    required name: str;
}

# Object with keyword links and props
type `limit` {
    `alter`: bool;
    `like`: str;

    `commit`: User;
    multi `configure`: User {
        `create`: bool;
    }
}
